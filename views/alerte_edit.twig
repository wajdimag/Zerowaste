{% extends "base.twig" %}

{% block title %}Modifier une alerte{% endblock %}

{% block content %}
  <h1>Modifier une alerte</h1>

  <hr>

  {% for alerte in alertes %}
    <form method="post">
      <div class="form-group">
        <label for="type_alerte">Type d'alerte:</label>
        <select class="form-control" id="type_alerte" name="type_alerte">
          <option value="{{ alerte.type_alerte }}" selected>{{ alerte.type_alerte }}</option>
          <option value="Bravo">Bravo</option>
          <option value="Normal">Normal</option>
          <option value="Alerte">Alerte</option>
          <option value="Urgence">Urgence</option>
        </select>
      </div>
      <div class="form-group">
        <label for="message">Message:</label>
        <textarea class="form-control" id="message" name="message" rows="3">{{ alerte.message }}</textarea>
      </div>
      <div class="form
